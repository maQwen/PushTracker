name: Test

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      GRADLE_OPTS: -Dorg.gradle.parallel=false

    steps:

      - name: Assign values
        env:
          KEY1: ${{ secrets.KEYSTORE_BASE64 }}
          MYMSG: "Simple message"
        run: 
          echo $KEY
          echo $MYMSG
          echo "Last line"

      - name: Check result
        if: ${{ env.KEY1 == '' }}
        run: echo "$MYMSG"
